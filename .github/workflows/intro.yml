name: Install && Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Show node version
        run: echo node -v
      - name: Instal dependencies
        run: npm install
      - name: Build App
        run: npm run build